// This is a basic Flutter integration test.
//
// Since integration tests run in a full Flutter application, they can interact
// with the host side of a plugin implementation, unlike Dart unit tests.
//
// For more information about Flutter integration tests, please see
// https://flutter.dev/to/integration-testing

// import 'package:flutter_test/flutter_test.dart';
// import 'package:integration_test/integration_test.dart';

// import 'package:auto_start_flutter/auto_start_flutter.dart';

void main() {
  // IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  // testWidgets('getDeviceManufacturer test', (WidgetTester tester) async {
  //   // The version string depends on the host platform running the test, so
  //   // just assert that some non-empty string is returned.
  //   final String? manufacturer = await getDeviceManufacturer();
  //   expect(manufacturer?.isNotEmpty, true);
  // });

  // testWidgets('isAutoStartAvailable test', (WidgetTester tester) async {
  //   final bool? isAvailable = await isAutoStartAvailable;
  //   // This can be true or false depending on device, but shouldn't throw.
  //   expect(isAvailable, isNotNull);
  //   // On iOS simulator it might be true (if BG refresh is enabled).
  // });
}
